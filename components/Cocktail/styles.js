import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    cocktailContainer: {
        width: '100%',
        height: '100%',
      },
      titles: {
        marginTop: '30%',
        width: '100%',
        alignItems: 'center',
      },
      title: {
        fontSize: 40,
        fontWeight: '600',
        color: '#fff'
      },
      subtitle: {
        fontSize: 16,
        color: '#fff'
      },
      image: {
        width: '100%',
        height: '100%',
        resizeMode: 'cover',
        position: 'absolute',
      }

})

export default styles;